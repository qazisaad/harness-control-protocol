
### Runner approval and input over HTTP

- Added combined coverage through production `NativeMcpBridge`, `HarnessMcpReview`, `McpAttachmentClient`, and the actual SDK HTTP transport. A local provider fixture advertises required review, requests form input, and returns a terminal result for accept, decline, and cancel responses.
- The scenarios assert zero tool dispatches before approval; the same review ID, action and hash on both signed HTTP bodies; persisted dispatching/input-resuming states before transmission; exactly one initial call and one continuation; completed state and native bridge result; and no private continuation state or form answer in public events.
- The signature callback is a fixture: these tests check body digest binding, not platform signature verification. They use the memory state store and local provider fixture, not Convex, workflow routing, the deployed hosted service, or a native model process. They do not satisfy those remaining combined acceptance or restart gates and do not enable managed review rollout.
- Validation: 63 focused runner tests passed across HTTP client, bridge, review, manager and persistence suites.

### Unreviewed native input survives process replacement

- Fixed a native lifetime bug: Codex threads were retained only when a discovered tool declared mandatory review. An unreviewed tool can still request protocol input, so its durable runner input record could survive while its ephemeral native thread disappeared. The native adapter now retains threads whenever the durable MCP owner has callable tools, independent of approval policy. Tool-free sessions keep the existing ephemeral behavior.
- Added `scripts/mcp-native-input-recovery-probe.mjs` in the isolated runner checkout. It runs the installed Codex app-server against a local model fixture, receives an unreviewed tool's form input, stops the native process, reloads the private operation from disk, applies the exact response, completes the continuation, then starts another native process on the saved thread. The model fixture consumes the injected completed operation through its final answer. The interrupted original native transport call remains distinct from that recovered operation.
- Reproduced with the old lifetime rule: `codex_request_failed: no rollout found for thread id`. With the corrected rule, the probe passed twice with one initial tool call, one continuation, two model requests, a persisted completed operation, and a terminal native answer. TypeScript build and 59 related adapter/manager/review/persistence tests passed.
- Scope: native OS-process replacement, production bridge/review owner and real disk storage are exercised. Model/tool responses are local fixtures, and the probe calls the durable owner directly after restart. Workflow/Convex, manager command routing in this same scenario, signed hosted dispatch, provider effects, deployment and coordinated release remain separate unproven boundaries. Managed review rollout remains gated.

### Session-manager recovery and reconnect ordering

- The native input recovery probe now routes its saved response through `HarnessSessionManager.recoverMcpReviews` and `respondToMcpInput`, using the production Codex adapter. It consumes the resulting `turn.completed` event, verifies the persisted operation is removed, and rejects a duplicate response without another tool call. This supersedes the prior probe's direct-owner recovery limitation; the initial interrupted native turn still starts through `runCodexTurn`, and the tool/model remain local fixtures.
- Fixed approval recovery marking an operation `dispatching` before session preparation. Approval and input now share one guarded preparation helper: validate the response without mutation, prepare the session, persist the decision, then dispatch. Failed preparation leaves a waiting operation retryable; competing responses cannot start a second preparation. If decision persistence fails after preparation, the prepared adapter and MCP clients are closed.
- A production manager regression test verifies invalid approval causes no connection, connection failure preserves the waiting record and sends no tool call, competing resumes are rejected, and the successful retry sends exactly one call and produces exactly one terminal event. Focused manager/review tests passed (31), and the real native process replacement probe passed through manager terminal cleanup. No deployment gate changed.

### Terminal cleanup after recovered dispatch failure

- The runner manager now owns MCP retirement at terminal-event emission. For the matching turn, the state store commits the terminal event and removes its private continuation atomically, then the manager removes the live review owner. The same path handles ordinary completion, cancellation and the connection watcher's exceptional `turn.failed` case. A failed provider call therefore cannot leave an apparently live reviewer waiting for a later session-stop command.
- The preparation/recovery regression covers both success and provider failure after dispatch, persisted retirement, exactly one dispatch, and rejection of late duplicate approval. Focused HCP tests passed (57); Playground review/hosted boundary tests passed (54); production Convex harness tests passed (61). The real native recovery probe still reaches manager terminal cleanup. The public consumer also passed from locally packed protocol/runner tarballs in a separate consumer directory, without internal imports or Playground dependencies. Packages remain unpublished.

### Legacy hosting reachability audit

- Removed 117 unused lines for AST extraction of tool/workflow source and legacy directory creation. These helpers had no execution callers; standalone code generation and local child hosting are not claimed removed by this cleanup.
- The local hosting path remains reachable from `/mcp-servers/id/{id}/start` and `/stop`, editor start/restart controls and debug-port configuration, local lab/discovery/status/log branches, cache generation, and subprocess lifecycle. Retiring only backend child startup would break these consumers. The coordinated cutover must update those entry points together, then drain and migrate persisted PID/port/status fields after the documented shared-service parity gate. The development Convex deployment selection is still pending from the user.
- Updated architecture guards to include the new skill snapshot and workspace binding compiler modules in the frozen import-boundary inventory. Replaced stale requirements for creation-time code generation and a removed discovery wrapper with checks matching asynchronous shared discovery and revision-only creation. Existing behavioral MCP status and artifact tests remain in the verification set.

### Worker-owned idle exit and read-only discovery (2026-09-21)

- The worker now owns active accounting for synchronous and retained jobs. Waiting input keeps capacity active. Idle expiry atomically closes admission, then shuts down the daemon cleanly; initial startup has at least 30 seconds of grace. Fly creation passes the idle interval and restart policy `no`. API-local release no longer schedules machine stops, and subsequent dispatch rechecks readiness.
- Local verification: 523 MCP/sandbox tests and three subtests passed before the discovery cleanup; the updated provider suite passed 38 tests. A real daemon retained a waiting input call beyond its idle interval, completed after response delivery, then exited with code zero. The same lifecycle probe passed in the rebuilt thin worker image under one CPU/512 MiB (`sha256:812c41ca3fb63dd2623b8963bd7533d0b46b11b9419b2e7c9b229a590254ebf3`).
- Machine discovery is now read-only: it neither destroys machines by age or local pool overflow nor adopts unowned same-image machines. Removed the obsolete stale-age setting. Discovery does not grant exclusive deletion ownership.
- Remaining limits: creation overflow, replacement and probe cleanup still need a complete cross-replica ownership audit. This is not live Fly shutdown/start evidence. Worker rollout, receipt-based dispatch/observation, remote child continuation, HCP integration and deployed acceptance remain incomplete; the managed nested gate stays closed.

### Shared machine cleanup ownership (2026-09-21)

- Removed shared execution-machine destruction from allocation overflow and replacement. A process-local allocation lock serializes local capacity checks and creation, eliminating the create-then-delete overflow race. A replacement changes only local routing; if startup fails, its local reservation is released without deleting the shared machine. This lock is not an app-wide capacity limit.
- Readiness probes create machines under a distinct `tool-sandbox-probe` owner that execution discovery rejects. Probe readiness waits on that exact machine and never allocates a replacement; final cleanup deletes only that dedicated probe. Probe timeout preserves its original failure. Removed the stale-age variable from the MCP environment forwarding list.
- Verification: 55 provider, retained-job and real daemon idle tests passed; scoped Ruff and whitespace checks passed. Tests cover concurrent allocation at capacity, failed replacement without destructive cleanup, actual API probe metadata consumed by production discovery, and exact probe cleanup after readiness failure.
- Remaining scope: shared stopped/obsolete machines now require deliberate deployment reconciliation instead of unsafe request-time deletion. Global allocation limits, live Fly rollout/cleanup acceptance, receipt-based provider recovery and nested managed integration remain outstanding. A lost machine-creation response may leave an undiscovered allocation; no exactly-once provisioning guarantee is claimed.

### Shared signed worker client (2026-09-21)

- Extracted daemon commands, result variants, identity and request fingerprint into one portable worker protocol module. Both daemon and client consume that source; the thin image copies it and passes its daemon import build check.
- Added a signed client for identity, submission, observation, response and cancellation against one physical endpoint. Commands are finite JSON and bounded to the daemon body limit; redirects are disabled. Returned worker identity and complete receipts must match. No client method provisions, retries a command or interprets a transport failure as permission to execute again.
- Focused verification: 16 client/retained-job/native idle tests passed. The real HTTP daemon/native subprocess test loses the accepted submission response, then recovers completion through observation with exactly one file effect and one submit. A mismatched boot receipt is rejected. The rebuilt worker image passes its import check.
- This adapter is not yet wired into hosted dispatch. Bind-before-submit, recovery from the durable row, canonical response authorization/validation, HCP ownership and deployed combined acceptance remain outstanding.

### Provider preparation and recovery separation (2026-09-21)

- Added a preparation context on the existing Fly backend: acquire/start/check a worker, read its signed identity, and return a bounded receipt plus client without submitting code. Local reservation is released even if the durable owner rejects binding. Preparation validates finite deadlines and logical IDs before allocation and rechecks the remaining deadline after readiness/identity.
- Existing-receipt recovery constructs a client directly for the receipt's physical worker. It never lists, creates, starts, healthchecks or replaces a Machine. Unreachable workers yield uncertainty; private HTTP is required and hostname identity is validated before routing.
- Verification: 46 provider/client tests passed, including no dispatch before binding, release after binding failure, zero provisioning calls on unreachable recovery, and invalid deadlines rejected before allocation. These are provider-boundary tests, not hosted invocation acceptance.
- Hosted gateway request construction remains canonical. Its dispatch currently still uses synchronous execution; integrating the preparation context with the existing invocation claim/binding and continuation mapping is next. No managed nested gate was lifted.

### Ordinary worker binding coordinator (2026-09-21)

- Added the ordinary owner's coordinator over the existing journal/provider interfaces. It requires a current claim, prepares without execution, awaits immutable binding, and submits only after binding succeeds. A lost submit response triggers observation of the bound worker, never another submit. An existing receipt is observation-only; missing workers cannot be recreated after protocol progress.
- Preparation runs in the bounded gateway sync executor. Caller cancellation during preparation waits for the in-flight preparation to unwind and releases its local reservation without binding or submitting. The coordinator returns typed worker observations; it does not create a second outcome ledger.
- Verification: 13 coordinator, binding bridge and worker client/provider tests passed. Controlled-boundary tests verify bind-before-submit ordering, rejected binding producing no client operation, existing receipt bypassing preparation, and cancellation releasing the acquired reservation. These tests do not exercise deployed Convex.
- Integration is still incomplete: the gateway's canonical request preparation must hand execution to this coordinator, and waiting/terminal observations must flow through hosted continuation/settlement. The production hosted dispatch has not switched and the nested managed gate remains closed.

### Canonical gateway preparation integration point (2026-09-21)

- Loaded-tool execution accepts a typed sandbox dispatcher and carries it explicitly through the gateway. Existing permit validation, secret/auth resolution, source wrapping, runtime config, capability/run tokens and nested references remain in their original owner. When supplied for a code tool, the dispatcher receives the final request and resolved policy; neither host execution nor the synchronous manager can bypass it.
- Verification: 103 gateway/hosted execution tests and 13 subtests passed. The new test consumes real loaded-spec preparation, validates the prepared source/arguments/broker token/config, and makes both old host and manager execution fail if called. Scoped Ruff and whitespace checks passed.
- The hosted executor still must supply the durable coordinator and translate waiting/terminal observations. This integration point alone does not enable retained production execution or close input/resume acceptance.

### Preserve worker uncertainty through the gateway (2026-09-21)

- Added an explicit unknown-outcome exception shared by worker transport/protocol failures. The gateway propagates it to the durable caller rather than converting it into a normal failed tool response. Existing hosted dispatch's uncertainty handler can then settle unknown instead of falsely claiming a known tool failure.
- The ordinary coordinator can observe a running receipt until it yields input/completion. It uses the original deadline and never prepares or submits during observation; expired running jobs and lost/evicted/cancelled result states remain unknown.
- Verification: 44 gateway/client/coordinator/hosted tests passed, including uncertainty propagation, original-receipt completion observation and expiration without provisioning. Scoped Ruff and whitespace checks passed. Production hosted dispatch still needs to select the coordinator and map native waiting input into a resumable protocol continuation.

### Native form reply bridge (2026-09-21)

- Native ordinary-input envelopes map to MCP form elicitation keyed by the exact interrupt ID. Replies must match the original request, be accepted, be finite JSON, and pass canonical schema validation. Decline/cancel produce an explicit declined outcome for the future owner integration. Secret-marked fields are rejected from plain forms and still require platform credential handling.
- Coordinator resume requires a current claim/original receipt, revalidates its journal binding, compares the actual waiting envelope and sends the validated response to that worker only. Lost response delivery falls back to observation, not resubmission.
- Verification: 20 native-input/coordinator/binding tests passed, covering values/enums/required/additional fields, changed prompts, decline/cancel, secret rejection and original-worker response delivery without preparation/submission.
- Still incomplete: hosted continuation persistence/dispatch selection, decline-to-worker cancellation, platform secret input handling, and combined native input-to-terminal acceptance. No production retained path or nested managed gate was enabled by this bridge.

### Ordinary hosted code dispatch is wired to retained workers (2026-09-21)

- Ordinary custom sandbox and code-execution exports now select the retained coordinator through canonical gateway preparation. Initial execution binds before submission; subsequent native input rounds use the original receipt. Native continuation state preserves the exact waiting envelope and checks it against the MCP prompt. Invalid replies are rejected before consuming the resume claim.
- Accepted replies pass through current publication/permit preparation and the coordinator's binding check. Decline/cancel targets the original worker and waits for a terminal observation; missing cancellation proof remains unknown. A completed worker requiring an unretained interaction also remains unknown rather than triggering source replay. Native result mapping shares the sandbox output-size limiter with the existing execution paths.
- Verification: 551 MCP/gateway/client/provider tests and 16 subtests passed; another 58 sandbox runtime/provider/coordinator tests passed after output-limit consolidation. The hosted round-trip test consumes suspension, continuation decode, reply, and terminal success with one initial dispatch. This test uses a controlled journal and coordinator responses; real daemon/client tests remain separate, so combined deployed/native/Convex acceptance is **not yet proven**. Signed nested-scope assertions now inspect the retained dispatch request, and remain intact.
- Remaining gaps: development Convex deployment selection/rollout, combined real-native hosted input acceptance, platform secret input handling, remote nested continuation, managed HCP integration, global provisioning limits and rollout cleanup. Changes exist only in the isolated checkout; no live service was deployed.


### Harness MCP authoring integration (2026-09-21)

- The live MCP-attached acceptance workflow exposed an authoring gap: harness semantic input still offered whole-server references, but the compiler requires explicit selections. Harness authoring now accepts `mcp_tools` with a server reference, tool name, exposure, and additional review requirement. Persistence writes canonical selections; projection preserves them and refuses unsupported attachments instead of silently dropping them.
- The adapter supplies the persisted schema's required empty `mcp_servers` array. A regression checks the real generated persistence model as well as the authoring/projection round trip. The initial live failure happened before workflow creation; the existing state/prompt resources were reused for the successful attempt.
- Live Dev publication succeeded for workflow `kx7d8b14x1t1vdqhmwdsd5frex8evjk8`, revision `rev_01a0c3cf-cca6-778a-8708-fdbf704bab8b`, runtime hash `2702304506d0b9ce4f3ebce4e89764a70a5f4e6f7952aa4980dd1dfa7099de4d`. It selects `sum_check` from dedicated server `jd7f2mq0bfctw12hcgpkr6mfnh8ev6qt`, requires approval, then requests a boolean confirmation inside native execution. Publication alone is not terminal execution evidence.
- Dispatcher configuration now carries the canonical resolved internal signing secret into its environment, including when the local auth settings use their existing fallback. Local object-store contents are excluded from image build contexts.
- Verification: 100 authoring tests, 33 dispatcher-provider tests, generated authoring checks, frontend TypeScript, and scoped authoring Ruff checks passed. The persisted-model regression was additionally rerun after the live schema mismatch was fixed.
- Remaining acceptance: run the selected tool through the real runner, dispatcher, review, native input, and terminal event. Managed nested execution, secret input, migration, and the other release gates remain open.

- Deployed current shared dispatcher through `MCPModels.adeploy_server_by_id` and the existing-machine provider, preserving machine `4d895360c67108` in `prompt2agent-mcp-dev`. Dispatcher image digest: `sha256:6f84247c9ee6b9cbf07c931c8dd98a8668b70d0c3b62ae54a37f8bba7f6a55dc`. Native worker image was published as `registry.fly.io/agentic-playground-sandbox-runner:mcp-native-20260921` (digest `sha256:8fff3972ec6c79de350b9fd0e2417528abe3401133552ecc8b0a4dcdef554d06`). Existing sandbox machines were not updated.
- The first real MCP-attached run, `37418a48-dd66-469c-b05e-ec8d40ed9d35`, reached hosted ingress and received HTTP 421. A Fly app-level environment value overrode the machine's public-origin setting. Staged the verified Fly origin and selected worker image on the development app, then reapplied the existing-machine deployment. `/health` now returns `shared_stateless`, environment `dev`, capacity 64.
- That failed initialization exposed a separate HCP 0.4.2 defect: the failed client's connection was not registered for cleanup, and rejected startup retained no `session.exited` evidence. The platform correctly left its invocation unknown and its host fenced. A runner-source fix now registers the client before connection, records cleanup only after every client closes, and sends the retained cleanup event even when the start command is rejected. Cleanup failure still provides no terminal cleanup claim; resume retains its existing original error behavior. HCP TypeScript and 36 focused runner/session/connection tests pass.
- The runner-source fix is **not yet published or installed in the live runner**. The old failed invocation remains unresolved; it was not manually marked closed. Therefore this is deployed dispatcher readiness and live failure diagnosis, not combined MCP approval/input/terminal acceptance. Release/install the corrected runner and complete safe recovery before rerunning the scenario.

- Prepared HCP 0.4.3 release artifacts and passed the full release checks, including clean artifact installation and public SDK approval/reload/resume/cleanup acceptance. The user completed npm browser authorization, and npm accepted protocol, SDK, and runner 0.4.3 uploads. npm reported asynchronous package processing; clean registry installation initially returned `ETARGET` because the version was not yet available. No platform package pins were advanced, and the live runner still uses 0.4.2. Verify clean registry availability before installing and continuing live recovery/acceptance; do not republish the accepted versions or substitute local tarballs as registry proof.

### Live harness interaction corrections (2026-09-21)

The new HCP 0.4.3 machine pairing is approved and running. Live testing reached tool approval and retained native input, but has not yet reached successful completion of the combined scenario. These findings supersede earlier entries describing pairing and hosted dispatch as unwired.

- A deliberate HCP interaction wait remains PENDING in the effect ledger. Workflow finalization excludes it from ambiguous-effect failure only when its registered invocation/request matches an actual pending graph interrupt; unrelated or stale pending effects still fail closed.
- Each harness interaction uses its own durable task interrupt identity. Resume commands target that identity explicitly, including single-interrupt commands. Recovery recognizes a subsequent interrupt within the same parent checkpoint without reapplying the previous decision.
- Hosted dispatcher provisioning selects the private HTTP worker transport required by retained jobs. The existing Dev dispatcher machine was updated; the next real run reached the native confirmation form.
- Input envelope validation now belongs to the shared backend contract, used by both the gateway and workflow adapter.
- A native input submission exposed reused terminal chat rows retaining the prior physical run ID. The terminal projection writer now updates the run identity of written rows and the turn anchor. Omitted rows remain preserved. The focused production-mutation regression passes, and the fix is deployed to `dev:tame-turtle-746`.

The failed live sessions were observed closed; they are not successful acceptance evidence. Pending gates include combined approval/input/result/cleanup, live UI interrupt hydration without refresh, and the broader migration, nested execution, secret input and deployment gates above.

#### Terminal result of the resumed live test

Conversation `8137f19319564245b2c45cbae943fcdc` reached approval, native input and a terminal response under HCP invocation `harness_ae1c098a8444d119354d7a69bd66d52d4021d63ca6056c1167b305b324c6fb8f`; the invocation closed. The terminal result was a tool error, **not 5**. Input admission correctly targeted the resumed run after the row ownership fix. The tool's default 30-second worker lifetime expired before the manual response, while the input form remained available. A subsequent attempt reproduced this mismatch and also closed.

The coordinator now rejects expired input replies as an unknown retained execution outcome before attempting worker binding or response dispatch; it never renews the receipt or replays the source. Eight coordinator tests pass, including the expired-reply regression. This last coordinator change is local and has not been deployed. Remaining work is to align the presented input lifetime with the retained execution deadline and complete successful combined acceptance with an appropriate explicitly configured interactive execution budget. Do not count these terminal error responses as successful MCP tool execution.

### Adapter-start cleanup evidence correction (2026-09-21)

Review confirmed that HCP 0.4.3 records cleaned MCP initialization failure, but not a later adapter-start failure after MCP has connected. The runner now routes both failures through one startup-cleanup result: confirmed client closure plus adapter stop records `session.exited` before rethrowing the original adapter error. Any cleanup failure withholds exit evidence and preserves the cleanup error; it does not release ownership on an unproven shutdown. Resume preparation still preserves the retained operation rather than emitting a fresh-session exit.

Regression coverage includes successful cleanup, client-close failure, adapter-stop failure, both failures, and real RunnerConnection delivery of exit evidence before command rejection. The correction is local to the HCP checkout and requires a new published runner release; installed 0.4.3 is not fixed by this source edit.

### HCP 0.4.4 release preparation (2026-09-21)

The coordinated protocol/SDK/runner 0.4.4 artifacts pass the full release test suite and clean packaged-consumer checks. A dedicated startup-cleanup consumer verifies successful closure plus each client/adapter cleanup failure, including durable event replay after reconstructing the manager. This consumer is now part of the release check.

Registry publication is waiting for the npm account owner's security-key/passkey verification. No 0.4.4 registry installation, platform pin update, or running test-runner replacement is claimed yet. The platform remains pinned to published 0.4.3 until registry publication succeeds.

### HCP 0.4.4 released and installed (2026-09-21)

npm publication completed for protocol, SDK and runner 0.4.4. After registry propagation, `/tmp/hcp-044-registry-consumer` installed all three exact versions from npm. The published startup-cleanup consumer passed all four closure outcomes and durable replay. The public MCP consumer passed approval/reload/resume/terminal cleanup, and the public SDK consumer passed its WebSocket scenario. These are controlled fault/adapter integration checks, not proof of the complete live native MCP calculation.

The isolated platform now pins protocol/SDK 0.4.4 in package.json and bun.lock; canonical HCP generation/check succeeds with the unchanged protocol schema hash. The isolated paired runner was replaced with the registry-installed 0.4.4 executable using its existing config/credentials, and the local control plane accepted its connection. No re-pairing, original-workspace service changes or production rollout was required. The adapter-start cleanup correction is therefore present in the running isolated runner. Remaining native-input deadline and broader MCP acceptance gates above are unchanged.

### HCP 0.4.5 registry and deadline rollout (2026-09-21)

All three exact packages installed from npm in `/tmp/hcp-045-registry-consumer` after registry processing completed. The released startup-cleanup, MCP review/reload and public SDK consumers passed. Playground package.json and bun.lock pin 0.4.5; backend protocol artifacts were regenerated from the installed package and pass the canonical check. The isolated paired runner uses the registry executable and the control plane accepted it. The original port-8000 runner is unchanged.

The Dev dispatcher remains machine `4d895360c67108`, updated through the existing provider path to image digest `sha256:43ee5b4e179047d03333bedf8b7c211415b019563d08fd20dfe27efd2c38ed0d`. Its worker image digest is `sha256:095ef25a900daac27c1b87a058137a3e319db2a5078f4fcfc42f529554625a85`. Five actual Linux worker tests passed under one CPU and 512 MiB. Image validation caught and fixed the missing canonical tool-input module and expiry timestamp rounding; 31 focused backend regressions pass.

The decline scenario on the new dispatcher (runner still 0.4.4 for that run) used thread `767c1ef3c384486784d46cba3e8ace4d`, runs `3f7aa977-b9da-4873-b2d8-eae560fcc90b`, `41f51642-3b35-4bfb-b734-5a7f70220b0d`, and terminal `f7342d6a-6c2c-4c5a-a3a2-db2598015004`. Invocation `harness_340b197825ee5a0b464d8a65ccee92448e5477b1077684592c29f978ce779339` consumed the decline error, emitted terminal turn sequence 87 and session exit 88, and is closed. This closes live decline and the latest activity-reset check, not the outstanding full product gates.
